[package]
name = "markdown-rust-py"
version = "0.1.0"
edition = "2024"

[lib]
name = "markdown_rust"
crate-type = ["cdylib"]

[dependencies]
markdown-rust-core = { path = "../core" }
pyo3 = { version = "0.29.2", features = ["extension-module"] }
